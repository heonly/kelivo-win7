name: Flutter Engine Win7 Build

on:
  workflow_dispatch:
    inputs:
      kelivo_ref:
        description: Kelivo commit/ref to build
        required: true
        default: c8c9ff37
  push:
    branches: [main]

jobs:
  test-trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Trigger received: ${{ github.event.inputs.kelivo_ref }}"
